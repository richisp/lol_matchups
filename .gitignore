.env
*.db-journal
*.db-wal
*.db-shm
*.db.tmp
*.log
__pycache__/
*.pyc
.venv/
.ruff_cache/
.mypy_cache/
.pytest_cache/
*.egg-info/

# Build / release artifacts
build/
dist/
installer/
*.update.bat
*.update.log

# Generated by sync.py and the crawler workflow respectively
db-version.json
db-version-body.md
# icon.ico is generated from heimerdinger-emote.webp by the PyInstaller spec
# but we commit it too so the build can never miss it (and to skip Pillow at
# release time on machines that don't have it). Regenerated on next build if
# the source .webp is newer.
